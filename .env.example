# Website cloud is Firebase now — see web/firebase-config.js
# (copy from web/firebase-config.example.js if needed).
#
# Legacy Supabase env vars are unused by the multi-page site:
# VITE_SUPABASE_URL=
# VITE_SUPABASE_PUBLISHABLE_KEY=
